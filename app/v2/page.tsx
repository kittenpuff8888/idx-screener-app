"use client";

import { ResearchDashboardFaithful } from "@/components/v2/pages/ResearchDashboardFaithful";

// Research Dashboard (/) — pixel-faithful port of "1. Research Dashboard.dc.html":
// MACRO econ rail + breadth internals + the real regime/risk read, cross-asset
// carousel, leaders/laggards, sectoral+konglo rotation, and squarified treemap.
export default function V2ResearchPage() {
  return <ResearchDashboardFaithful />;
}
