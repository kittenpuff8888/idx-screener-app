"use client";

import { ScreenerFaithful } from "@/components/v2/pages/ScreenerFaithful";

// Screener (/screener) — pixel-faithful port of "2. Screener.dc.html", wired to
// the real workbook (lib/v2/screenerData).
export default function V2ScreenerPage() {
  return <ScreenerFaithful />;
}
